# Laravel API (relative — proxied via Next.js rewrites in next.config.ts)
NEXT_PUBLIC_API_URL=/api/v1

# Backend proxy target (used by next.config.ts rewrites)
API_PROXY_TARGET=https://codemafia.ng

NEXT_PUBLIC_SITE_URL=https://codemafia.ng
